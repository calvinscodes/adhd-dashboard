{
  "buildCommand": "pnpm --filter web build && cp -r apps/web/dist/. dist",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "functions": {
    "api/server.ts": {
      "runtime": "nodejs20.x",
      "memory": 512
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/server" }
  ]
}
