{
  "name": "adhd-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "dev:web": "pnpm --filter web dev",
    "dev:electron": "pnpm --filter electron start",
    "build": "pnpm --filter web build",
    "build:electron": "pnpm --filter electron build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
